package com.projectapp.lessons._6_Exception;


/**
 * Операторы try могут быть вложенными. Это означает, что один оператор try
 * может находиться в блоке друтого оператора try.
 * Всякий раз, когда управление передается блоку оператора try
 * Если во вложенном операторе try отсутствует оператор catch
 * для перехвата и обработки конкретного исключения, стек развертывается и проверяется
 * на соответствие оператор саtch из внешнего блока оператора try, и так
 * до тех пор, пока не будет найден подходящий оператор саtch или не будут исчер­
 * паны все уровни вложенности операторов try.
 */


public class A3_Exception {
    public static void main(String[] args) {
        //        try {
//            int a = args.length; //a=1; a=2;
//            int b = 45 / a;
//            System.out.println(" a = " + a);
//
//            try {
//                if (a == 1)
//                    a = a / (a - a);
//
//                if (a == 2) {
//                    int[] c = {1};
//                    c[49] = 100;
//                }
//            } catch (ArrayIndexOutOfBoundsException e) {
//                System.out.println("Вышли за пределы массива!");
//            }
//        } catch (ArithmeticException e) {
//            System.out.println("Деление на ноль!");
//        }

        /** если программа запускается на выполнение с одним
         аргументом, то исключение в связи с делением на нуль генерируется во вложенном
         блоке оператора try. Но поскольку это исключение не обрабатывается во
         вложенном блоке, то оно передается внешнему блоку оператора try, где и обрабатывается*/

    }
}
